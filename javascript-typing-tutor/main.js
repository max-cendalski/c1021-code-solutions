
/*  var spanIndex = 0;
var keyDownCount = 0;
var numberOfMistakes = 0;
var comments = ['what', 'whatever'];

$spanKey = document.querySelectorAll('span');
console.log($spanKey[1].innerText)

function handleTyping(event) {
  if (event.target === $spanKey[1]) {
    console.log('event.key', event.target);
    console.log('bingo')

  }
  console.log('event.target', event.target)
}

var $typingSection = document.querySelector('.typing-section');
$typingSection.addEventListener('click', handleTyping);
 */
